TITLE ERRSET	SET MAXIMUM ERROR COUNT
SUBTTL	19-JUN-70	T EGGERS/DMN
;FROM ERROR.MAC V.021

	ENTRY	ERRSET
	EXTERN	ERRMAX
	Q=16

	SIXBIT /ERRSET/
ERRSET:	0
	MOVE	0,@(Q)		;GET MAXIMUM ERROR NUMBER
	MOVEM	0,ERRMAX	; AND STORE AWAY
	JRA	Q,1(Q)		;RETURN

	END
